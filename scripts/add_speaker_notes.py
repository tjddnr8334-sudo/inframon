#!/usr/bin/env python3
"""슬라이드마다 **발표자가 실제로 말할 말**을 노트에 넣는다.

요약이 아니라 **대본**이다. 그대로 읽어도 말이 되고, 눈으로 훑어도 어디서 무엇을
강조할지 보이게 쓴다. 자료 전체가 "성능은 두루뭉실, 목적은 분명" 으로 가고 있으므로
대본도 같은 자리에 선다 — **한계를 우리 입으로 먼저 말하고**, 숫자를 뺀 자리는
왜 뺐는지 말로 채운다.

아픈 질문은 미리 적어 둔다(`※`). 현장에서 나올 것이 뻔한 셋이다.
  · 3D 인데 높이는 믿을 만한가 (5쪽)
  · 계측과 맞춰 봤나 (8쪽)
  · 두 칸의 점 수가 왜 다른가 (8쪽)

    python scripts/add_speaker_notes.py --deck <8쪽짜리 vF 형식 pptx>

산출: 대상 pptx 의 슬라이드 노트
"""

from __future__ import annotations

import argparse
from pathlib import Path

from pptx import Presentation

ROOT = Path(__file__).resolve().parent.parent
VF = ROOT / "docs" / "KICT_Bmaps_Inframon_연구개발_7p_vF.pptx"
NL = chr(10)

NOTES = {
1: """[여는 말]
안녕하십니까. 오늘 말씀드릴 것은 위성으로 교량 변위를 재는 플랫폼, Inframon 입니다.

한 줄로 말씀드리면 — 계측기가 달리지 않은 교량도 움직임을 재서, KICT B-Maps 에
얹자는 이야기입니다.

순서는 네 장입니다. 왜 필요한가, 무엇을 목표로 하는가, 지금 무엇을 만들었는가,
그리고 앞으로 어떻게 할 것인가.

먼저 왜 필요한지부터 말씀드리겠습니다.""",

2: """[문제를 세우는 장]
지금 우리는 다리의 일부만, 그것도 가끔만 보고 있습니다.

센서는 일부 교량의 일부 지점에만 붙어 있습니다. 성수대교와 한강대교조차 처짐
센서가 없습니다. 센서가 없는 다리는 점검과 점검 사이가 통째로 빈칸이고,
지나간 시간은 되돌려 볼 수 없습니다.

위성은 그 빈칸을 채웁니다. 같은 자리를 12일마다 다시 찍고, 다리에는 아무것도
달지 않으며, 자료는 전량 무상입니다.

★ 여기서 꼭 말할 것 — 2018년 6월까지 거슬러 올라가 다시 볼 수 있다는 점입니다.
센서는 단 뒤부터만 보이지만, 위성은 이미 찍어 둔 것이 있습니다. 이게 가장 큰
차이입니다.

아래 표가 그 차이를 정리한 것입니다. 계측기가 있는 교량은 교차검증이 되고,
없는 교량은 12일마다 전 구간이 갱신됩니다.""",

3: """[목표를 말하는 장]
최종 목표는 계측기가 없는 교량까지 위성으로 상태를 파악하는 체계를 만들고,
그것을 B-Maps 에 연계하는 것입니다.

세부 목표는 넷입니다 — 위성 변위 자동 산출, 물리 기반 AI 해석, 교량 디지털 트윈,
그리고 B-Maps 연계 서비스.

★ 표 가운데 칸을 봐 주십시오. '현재 달성' 이 아니라 **'지금 단계'** 라고 적었습니다.
개소 수나 비율을 적지 않았습니다. 아직 정확도를 말씀드릴 단계가 아니기 때문입니다.
정확도 지표는 계측 대조와 측점 밀도를 확보한 뒤에 내겠습니다.

CRI 도 '기준 잠정' 이라고 적어 두었습니다. 안전등급이 양호한 교량의 실측값으로
기준을 다시 잡아야 확정됩니다. 지금 등급을 구조 상태로 읽으시면 안 됩니다.""",

4: """[무엇을 만들었는지 — 그리고 못 하는 것]
좌표 하나만 넣으면 여섯 단계가 사람 손 없이 돕니다. 영상 고르기, InSAR 처리,
교면 측점 정합, PINN 역산, 위험도 평가, 그리고 산출·연동입니다.

왼쪽 특징 가운데 '근거 기록' 을 봐 주십시오. 관측 조건과 신뢰구간을 결과에 **항상
함께** 남깁니다. 값만 던지면 받는 쪽이 그걸 믿어도 되는지 알 수 없기 때문입니다.

★ 오른쪽은 **저희가 못 하는 것**입니다. 이 자리에서 먼저 말씀드리는 게 맞다고
봤습니다.

 · 교면 측점 밀도 — 위성 화소가 지상 5×20 m 인데 상판 폭은 1~2 화소입니다.
   프로그램으로 늘릴 수 없습니다. 고해상도 위성이나 코너리플렉터가 답입니다.
 · 절대 강성 EI — 위성은 상대 변위만 보므로 관측으로 낼 수 없습니다.
   지금 값은 설계 제원으로 계산한 것입니다.
 · 연직 변위 — 한 궤도로는 위성 시선 방향만 나옵니다. 두 궤도를 합쳐야
   위아래가 됩니다.""",

5: """[실증 — 교량마다 같은 것이 나온다]
교량마다 같은 산출물과 IFC 트윈이 자동으로 나옵니다. 표는 한강 교량들이고,
관측 시점 수와 교면 측점 수가 교량마다 다릅니다.

★ 여기서 **변위속도를 일부러 빼 두었습니다.** 지금 측점으로 낸 속도는 대부분
95 % 구간이 0 을 품습니다. 숫자를 적어 두면 확정된 관측처럼 읽히기 때문에,
측점 밀도를 확보한 뒤에 제시하겠습니다. 물어보시면 그대로 말씀드리면 됩니다.

아래는 교량 한 곳당 자동으로 나오는 여섯 가지입니다. 오른쪽 그림이 그중 IFC
트윈이고, 점 색이 위성 시선 방향의 변위 속도입니다.

※ "3D 인데 높이는 정확한가" 물으시면 —
높이는 DEM 에 위성이 추정한 잔차고도를 더한 값이고, 그 불확실도가 교량마다
7~22 m 입니다. 주탑처럼 30 m 이상 차이 나는 것은 가르지만, 교각·교대는 못
가립니다. 그래서 교면은 평면 위치로 고르고, 높이는 검산으로만 씁니다.
실제로 그 검산이 일한 적이 있습니다 — 한 교량에서 '교면 위' 점들이 지면보다
44 m 낮게 나와, 그게 교면이 아니라 강변 지반임을 잡아냈습니다.""",

6: """[실제로 돈다 — 그리고 어떻게 붙는가]
이건 개념도가 아니라 **실제로 도는 화면**입니다. 성수대교, 관측 101회, 교면 측점
103개. 지도에서 점을 하나 누르면 그 자리의 변위 이력이 오른쪽에 뜨고, 아래는
위험 지수 추세입니다. CSV 로 바로 내려받습니다.

★ 붙이는 방식이 중요합니다. **B-Maps 본체는 한 줄도 고치지 않습니다.**
옆에 붙는 조회 전용 프로그램이 교량관리번호로 물어보면 답하는 구조입니다.

그래서 세 가지가 됩니다.
 · B-Maps 개발팀이 손댈 것이 없습니다.
 · 저희 쪽이 멈춰도 B-Maps 는 멀쩡합니다. 위성 탭만 비고 나머지는 그대로입니다.
 · 마음에 안 드시면 그냥 끄시면 됩니다. 저희는 읽기만 하고 쓰지 않습니다.

오른쪽 위는 측점마다 나오는 값 네 가지와 대기 보정입니다.

그 아래 '한계' 를 한 번 더 말씀드립니다 — 부족한 것은 방법이 아니라 **교면 측점의
수**입니다. 주경간에 코너리플렉터를 놓거나 고해상도 위성을 쓰면 채워집니다.""",

7: """[앞으로 — 그리고 부탁]
개발·실증은 진행했고, 시범 연동은 협의가 필요하며, 운영·고도화가 남았습니다.

운영 단계에서 PINN 이 본격적으로 일합니다 — 성능 평가, 노후도 추정, 앞으로의
거동 예측입니다. 지금은 변위를 열·하중·침하·이상으로 나누는 데까지 와 있고,
그 나눔이 맞는지 계측으로 검증하는 것이 다음 일입니다.

★ KICT 께 드리는 요청은 **자료 네 가지뿐**입니다. 새로 만들어 주실 것은 없고,
B-Maps 가 이미 들고 계신 것들입니다.

 1 교량 대장 제원 — 연장·폭·경간·형하고. 지금 저희는 공개데이터와 지도로
   추정합니다. 교면 측점을 가려내는 폭이 이 값에서 나오므로, 제원이 정확해지면
   저희 결과가 **바로** 좋아집니다. 넷 중 이게 제일 급합니다.
 2 교량관리번호와 대장 좌표 — 결과를 대장에 붙이는 열쇠입니다.
 3 보유하신 IFC 모델 — 있는 교량만이라도. 그러면 저희 트윈이 아니라 B-Maps 의
   부재에 값이 바로 붙습니다.
 4 준공·보수 이력 — 연도와 일자만. 변위에 계단이 보일 때 보수인지 이상인지
   가릅니다.

맨 아래 — **연동 시연은 지금 바로 가능합니다.** 레지스트리에 교량관리번호만
넣어 주시면 그 교량이 B-Maps 탭에 뜹니다.""",

8: """[부록 — 물으시면 꺼내는 장]
성수대교 한 곳을 끝까지 펴 본 것입니다. 관측 101회, 교면 측점 15점.

★ 가장 말씀드리고 싶은 것은 오른쪽 두 번째 줄입니다. **열팽창이 교면과 지반에서
갈라집니다** — 교면은 +0.15, 지반은 +0.41 mm/°C 입니다.
"교면을 보고 있는 게 맞느냐" 가 이 과제에서 가장 아픈 질문인데, 여기서는 그 답이
숫자로 나옵니다. 위성이 다리를 본 것이지 강변 땅을 본 것이 아니라는 뜻입니다.

왼쪽은 처리 쪽입니다. 536점 가운데 시간결맞음 문턱을 넘은 204점을 썼고,
대기 보정으로 잔차가 16.3 에서 11.2 mm 로 줄었습니다.

※ "두 칸의 점 수가 왜 다른가" 물으시면 —
왼쪽은 MT-InSAR 전체 처리이고 오른쪽은 교면만 다시 고른 것이라, 서로 다른
단계입니다. 그래서 점 수도 속도도 다릅니다.

※ "계측과 맞춰 봤나" 물으시면 —
맞춰 봤습니다. 계측 곡선이 있는 5개소 가운데 올림픽대교에서 R² 0.798 이 나왔고,
달을 섞어 2만 번 돌린 우연 기준선 0.607 을 넘습니다. 다만 교면 20점 가운데 그
선을 넘은 점이 **1개뿐**이라 아직 일치한다고 주장하지 않습니다. 부족한 것은
방법이 아니라 측점의 수라고 보고 있습니다.""",
}


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--deck", default=str(VF))
    ap.add_argument("--dry-run", action="store_true")
    a = ap.parse_args()

    prs = Presentation(a.deck)
    n = len(prs.slides)
    if n != len(NOTES):
        print(f"!! 이 대본은 {len(NOTES)}쪽짜리용인데 자료는 {n}쪽이다 — 멈춘다")
        return 1

    for i, s in enumerate(prs.slides, start=1):
        body = NOTES[i].strip()
        head = next((sh.text_frame.text.split(NL)[0]
                     for sh in s.shapes
                     if sh.has_text_frame and sh.text_frame.text.strip()), "")
        print(f"  {i}쪽  {head[:34]:<36} 노트 {len(body)}자")
        if not a.dry_run:
            s.notes_slide.notes_text_frame.text = body

    if a.dry_run:
        print("(dry-run — 저장하지 않았다)")
        return 0
    prs.save(a.deck)
    print(f"노트를 넣었다: {a.deck}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
