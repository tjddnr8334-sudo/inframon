# 정자교 — 좌표 하나로 끝까지 (bridge_run)

`37.3685405, 127.1089983` · 트랙 `sarvey_permissive.h5`

## 무엇을 어디서 가져왔나

| 항목 | 출처 |
|---|---|
| specs | 파트너 실측 CSV '정자교' (8 m) |
| bridge_type | CSV 'PC슬래브교(PCS)' → slab |
| deck_match | CSV 연장 108 m 에 맞는 way 선택(4후보 중) |
| deck | OSM '정자교' 110 m · 방위 0.4° |
| width | OSM 보도 간격 23.5 m + 보도 반폭 |
| ground | Open-Meteo DEM |
| points | 쉬프트 6.8 m(heading -13.2°) 보정 후 데크 ±30 m 안 12/2661 |
| clearance | 잔차고도 집단평균 +5.2±1.2 m (z=4.31) |

## 제원(적용값)

| 연장 | 경간 | 폭 | 형하고(δh) | 지면 | 데크 방위 | 형식(PINN PDE) | 시점 |
|---|---|---|---|---|---|---|---|
| 108.0 m | 5 | 26.5 m | 5.2 m | 39.0 m | 0.4° | slab · prestressed_concrete | 201 |

## 결과

| | 값 |
|---|---|
| IFC 트윈 | 부재 11 · 점 12 · 결합 12 |
| 잔차고도 | 교면 위 − 밖 +5.2 ± 1.2 m (z=4.31) — 교면 위 점이 지면 점보다 유의하게 높다(z>2) |
| PINN · CRI | CRI 0.546 · 정상 |
| 감사 | **조건부** · 표준데이터에 이 교량이 없다(최근접 '금곡교' 565m). PINN 은 파트너 실측 CSV '정자교' (8 m) 제원(경간 108m)을 썼다 — 실 제원 확인 필요(--bridge-profile) |

ⓘ EI·고유진동수는 관측값이 아니라 **설계 제원(기하 EI) 기반** — InSAR 는 상대 변위라 절대 강성을 식별할 수 없다(f₁ 6.47Hz 는 물리 범위 안)

## 그림

![brief](brief.png)

3D: `twin.viewer.html` (속도) · `twin_cri.viewer.html` (CRI) — 더블클릭

## 적어 둘 것

- CSV 에 폭 없음 → OSM 보도 간격으로
- 형하고 6.0 m 가정(표준데이터 교량높이 없음) — 잔차고도로 대체 시도

## 이 파이프라인이 원리상 못 하는 것 (모든 교량 공통)

- **EI(강성) 관측 식별** — InSAR 는 상대 변위라 자중 처짐을 못 본다. EI·f₁ 은 설계 제원 기반이다(감사 ⓘ).
- **데크 위 PS 밀도** — Sentinel-1 화소 ~11 m. 프로그램으로 늘릴 수 없다. 고해상도 SAR·코너리플렉터가 답이다.
- **쉬프트 방향(각도)** — 궤도 heading 이 정한다. 크기 δh 만 잔차고도로 관측값이 된다.
- **시점 수** — 속도 95 % CI 는 시점 수와 기간이 정한다(201시점). 브리프 기준(≥100장)에 못 미치면 판정을 보류한다.
