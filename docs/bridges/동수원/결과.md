# 동수원 — 좌표 하나로 끝까지 (bridge_run)

`37.27754, 127.02956` · 트랙 `dongsuwon_track.h5`

## 무엇을 어디서 가져왔나

| 항목 | 출처 |
|---|---|
| specs | 파트너 실측 CSV '동수원고가차도' (113 m) |
| bridge_type | CSV '강박스거더교(STB)' → box_girder |
| clearance | 전국교량표준데이터 교량높이 6.8 m |
| deck | OSM '동수원고가차도' 1227 m · 방위 86.8° |
| width | 전국교량표준데이터 교량폭 17 m |
| ground | Open-Meteo DEM |
| ground_offset | 처리 오프셋 ≈ 0 (|B| 0.6 m < 3 m · 지면 점 342 · 도로 양쪽 대칭) |
| points | 쉬프트 7.8 m(heading -13.1°) 보정 후 데크 ±30 m 안 77/454 |
| span_layout | measured — 주경간 70 m(실측)를 가운데, 나머지 15경간 55 m 균등 |
| bind_warn | 결합 0/77 — 데크선이 크게 굽어 있다(현 대비 편차 15 %) — 직선 프록시가 곡선 위 측점을 따라가지 못한다 |

## 제원(적용값)

| 연장 | 경간 | 폭 | 형하고(δh) | 지면 | 데크 방위 | 형식(PINN PDE) | 시점 |
|---|---|---|---|---|---|---|---|
| 890.0 m | 16 | 17.0 m | 6.8 m | 57.0 m | 86.8° | box_girder · steel | 18 |

## 결과

| | 값 |
|---|---|
| IFC 트윈 | 부재 33 · 점 77 · 결합 0 |
| 잔차고도 | 없음(SNAP star 산출물 필요) |
| PINN · CRI | CRI 0.702 · 정상 |

ⓘ EI·고유진동수는 관측값이 아니라 **설계 제원(기하 EI) 기반** — InSAR 는 상대 변위라 절대 강성을 식별할 수 없다(f₁ 1.07Hz 는 물리 범위 안)

## 그림

![brief](brief.png)

3D: `twin.viewer.html` (속도) · `twin_cri.viewer.html` (CRI) — 더블클릭

## 적어 둘 것

- 대상 교량 30m 내 0점(최근접 165.0m)
- CSV 에 폭 없음 → OSM 보도 간격으로
- OSM 어느 way 도 CSV 연장 890 m 와 안 맞음 — '동수원고가차도' 1227 m 사용
- 처리 오프셋 ≈ 0 → 교량 위 점의 횡방향 치우침은 산란체 위치(보도·난간)로 본다
- SNAP star 처리 폴더도 SARvey 기선 JSON 도 없음 → 잔차고도 불가(브리프 (b) 비움)
- ⚠ 부재 결합 0/77 — 데크선이 크게 굽어 있다(현 대비 편차 15 %) — 직선 프록시가 곡선 위 측점을 따라가지 못한다. 부재별 통계를 쓰면 안 된다(교면 전체 통계는 유효)

## 이 파이프라인이 원리상 못 하는 것 (모든 교량 공통)

- **EI(강성) 관측 식별** — InSAR 는 상대 변위라 자중 처짐을 못 본다. EI·f₁ 은 설계 제원 기반이다(감사 ⓘ).
- **데크 위 PS 밀도** — Sentinel-1 화소 ~11 m. 프로그램으로 늘릴 수 없다. 고해상도 SAR·코너리플렉터가 답이다.
- **쉬프트 방향(각도)** — 궤도 heading 이 정한다. 크기 δh 만 잔차고도로 관측값이 된다.
- **쉬프트의 세 층** — A 기하(δh/tanθ, 보정) · B 처리 오프셋(지면 점 vs OSM 도로선으로 추정, 유의하면 제거) · C 산란체 위치(보도·난간 — 쉬프트가 아님). B 는 OSM 기준 상대값이라 3~5 m 아래는 못 본다.
- **시점 수** — 속도 95 % CI 는 시점 수와 기간이 정한다(18시점). 브리프 기준(≥100장)에 못 미치면 판정을 보류한다.
