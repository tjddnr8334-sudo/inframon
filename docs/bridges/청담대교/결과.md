# 청담대교 — 좌표 하나로 끝까지 (bridge_run)

`37.525609, 127.063966` · 트랙 `track_청담대교.h5`

## 무엇을 어디서 가져왔나

| 항목 | 출처 |
|---|---|
| specs | 파트너 실측 CSV '청담대교' (10 m) |
| bridge_type | CSV '미등록' → girder |
| deck_match | CSV 연장 1212 m 에 맞는 '청담대교' way 선택(5후보 중) |
| deck_trim | 곡선 램프 절단 → 본교 982 m |
| deck | OSM '청담대교' 1129 m · 방위 74.8° |
| width | OSM lanes=3 × 3.5 m + 여유(추정) |
| ground | Open-Meteo DEM |
| ground_offset | 처리 오프셋 ≈ 0 (|B| 0.0 m < 3 m · 지면 점 39 · 도로 양쪽 대칭) |
| points | 쉬프트 6.8 m(heading -13.3°) 보정 후 데크 ±30 m 안 20/20000 |
| span_layout | measured — 주경간 90 m(실측)를 가운데, 나머지 32경간 28 m 균등 |

## 제원(적용값)

| 연장 | 경간 | 폭 | 형하고(δh) | 지면 | 데크 방위 | 형식(PINN PDE) | 시점 |
|---|---|---|---|---|---|---|---|
| 982.4 m | 33 | 13.5 m | 6.0 m | 4.0 m | 74.8° | girder · steel | 51 |

## 결과

| | 값 |
|---|---|
| IFC 트윈 | 부재 67 · 점 20 · 결합 16 |
| 잔차고도 | 교면 위 − 밖 +5.8 ± 7.9 m (z=0.74) — 고도 차이를 유의하게 확인하지 못함(|z|≤2) — 감도 부족 또는 차이 없음 |
| PINN · CRI | CRI 0.786 · 경고 |

ⓘ EI·고유진동수는 관측값이 아니라 **설계 제원(기하 EI) 기반** — InSAR 는 상대 변위라 절대 강성을 식별할 수 없다(f₁ 0.87Hz 는 물리 범위 안)

## 그림

![brief](brief.png)

3D: `twin.viewer.html` (속도) · `twin_cri.viewer.html` (CRI) — 더블클릭

## 적어 둘 것

- 대상 교량 30m 내 0점(최근접 137.4m) · 표준데이터에 이 교량이 없다(최근접 '탄천2고가교' 549m). PINN 은 파트너 실측 CSV '청담대교' (10 m) 제원(경간 982m)을 썼다 — 실 제원 확인 필요(--bridge-profile) · CRI 등급은 **잠정** — 관측조건이 기준치 학습 조건과 다르다(관측기간 2748d vs 기준 552d). 등급을 구조 상태로 읽으면 안 된다
- CSV 에 폭 없음 → OSM 보도 간격으로
- 데크선 끝에 방위가 크게 꺾이는 구간이 있었다(곡선 접속램프) — 본교 982 m 만 남긴다
- 폭 추정 13.5 m — 실측 아님
- 경간수 없음 → 30 m 규칙으로 33 가정
- 형하고 6.0 m 가정(표준데이터 교량높이 없음) — 잔차고도로 대체 시도

## 이 파이프라인이 원리상 못 하는 것 (모든 교량 공통)

- **EI(강성) 관측 식별** — InSAR 는 상대 변위라 자중 처짐을 못 본다. EI·f₁ 은 설계 제원 기반이다(감사 ⓘ).
- **데크 위 PS 밀도** — Sentinel-1 화소 ~11 m. 프로그램으로 늘릴 수 없다. 고해상도 SAR·코너리플렉터가 답이다.
- **쉬프트 방향(각도)** — 궤도 heading 이 정한다. 크기 δh 만 잔차고도로 관측값이 된다.
- **쉬프트의 세 층** — A 기하(δh/tanθ, 보정) · B 처리 오프셋(지면 점 vs OSM 도로선으로 추정, 유의하면 제거) · C 산란체 위치(보도·난간 — 쉬프트가 아님). B 는 OSM 기준 상대값이라 3~5 m 아래는 못 본다.
- **시점 수** — 속도 95 % CI 는 시점 수와 기간이 정한다(51시점). 브리프 기준(≥100장)에 못 미치면 판정을 보류한다.

## 기선망과 시간결맞음

점마다 `los = c + v·t + K·Δh (+ α·ΔT)` 를 같이 푼다. K 는 수직기선이 정하므로
**기선이 얼마나 벌어져 있는지가 잔차고도를 가를 수 있는지를 정한다.** γ 는 그 모형이
그 점을 얼마나 설명하는지이고, 값보다 **어디 있는 점이 살아남았는지**가 중요하다.

| 항목 | 값 |
|---|---|
| 처리 사슬 | SNAP |
| 시점 · 마스터 | 51시점 · 20220330 |
| 수직기선 B⊥ | -189.0 ~ +104.5 m (폭 294 m · σ 68.7 m) |
| 시간기선 | 2748 일 |
| 기선망 | 쌍 278개 · 차수 최소 6 · 평균 10.9 · **연결됨** (문턱 B⊥ ≤ 150 m · Bt ≤ 400 일) |
| 높이 모호성 | B⊥=100 m 에서 165 m |
| **γ 중앙값** | **0.422** (문턱 0.35 → 90/151점 통과 (60 %)) |
| γ 통과 점이 어디 있나 | 교량 위(0~10 m) 5/6점 · 먼 맨땅(100~200 m) 34점 |
| 잔차고도 σΔh | 10.6 m — 형하고(8~25 m)보다 작다: 높이로 가려낼 여지가 있다 |
| APS 제거량 | RMS 1.92 mm · 잔차 13.9 → 10.2 mm |

![기선망](mtinsar_baseline.png)

시점별 B⊥·시간기선은 `mtinsar_baselines.csv`, 점별 속도·Δh·열팽창·γ 는
`mtinsar_points.csv` 에 있다(SARPROZ 가 내는 표와 같은 꼴).
